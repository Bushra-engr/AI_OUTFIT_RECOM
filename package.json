{
  "name": "ai-outfit-recom",
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix react install && npm --prefix react run build && node -e \"const fs = require('fs'); if (fs.existsSync('dist')) fs.cpSync('dist', 'backend/static', {recursive: true});\""
  }
}
